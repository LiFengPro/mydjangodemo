 version: '2'
 services:
   db:
     image: postgres
   web:
     image: lifeng2/mydjango
     build: .
     volumes:
       - .:/code
     ports:
       - "4099:8000"
     depends_on:
       - db
     command: ["./wait-for-it.sh", "db:5432", "--", "sh", "setup_production.sh"]